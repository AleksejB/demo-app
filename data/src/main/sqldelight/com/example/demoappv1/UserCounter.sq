CREATE TABLE user_counter_table (
    user_id INTEGER AS Int NOT NULL PRIMARY KEY,
    count INTEGER AS Int NOT NULL DEFAULT 0
);

getUserByUsername:
SELECT * FROM user_counter_table WHERE user_id = :userId;

updateUserCountByUsername:
UPDATE user_counter_table
SET count = :newCount
WHERE user_id = :userId;

insertNewUser:
INSERT OR IGNORE INTO user_counter_table VALUES(:userId, :count);